# Architecture Decision Records

This directory records **decisions and changes** for `colonel_kernel`. It complements the other docs (see `CLAUDE.md` and `FOUNDATIONS.md` for the full structure):

- **`FOUNDATIONS.md`** — settled foundations and the reasoning behind them (the canonical source of truth).
- **ADRs (here)** — individual decisions and changes, recorded as they are made.
- **`NEXT_SESSION.md`** — the immediate working state and next actions.

When an ADR changes a settled point in `FOUNDATIONS.md`, update `FOUNDATIONS.md` to match so the two never disagree, and have them cross-reference each other.

## Convention

- One decision per file.
- Filenames are numbered and kebab-cased: `NNNN-title-in-kebab-case.md` (e.g. `0001-delta-rasterization.md`), zero-padded to four digits, incrementing.
- Each ADR follows the **Michael Nygard template**: **Title**, **Status**, **Context**, **Decision**, **Consequences**.
- **Status** is one of: `Proposed`, `Accepted`, `Deprecated`, or `Superseded by ADR-NNNN`.
- ADRs are immutable once Accepted: to change a decision, write a new ADR that supersedes the old one (and update the old one's status), rather than editing history.
- **In-place amendments are permitted only when they change no decision** — dated, attributed pointer notes: delivered-by, corrected-by, or a recorded implementation divergence. ADR-0019 already carries two (its clock convention "amended in place rather than superseded", and the ADR-0036 install-source correction), and ADR-0038 added a third. Anything that changes what was decided still needs a new superseding ADR.

## Index

| ADR | Title | Status |
|-----|-------|--------|
| [0001](0001-delta-rasterization.md) | Delta rasterization: snap + unit-amplitude defaults | Accepted |
| [0002](0002-global-timebase.md) | Global timebase (authored-adjustable, load-locked) | Accepted |
| [0003](0003-kernel-source.md) | Kernel source — parameterized library, no freehand drawing | Accepted |
| [0004](0004-tab2-deconvolution-method.md) | Tab 2 deconvolution method — regularized least squares ported from deconvreg | Accepted |
| [0005](0005-tab2-sta-validation-partner.md) | STA as Tab 2 cross-method validation partner | Accepted |
| [0006](0006-linear-convolution.md) | Linear convolution (zero-padded) as the default convention | Accepted |
| [0007](0007-build-order.md) | Build order / tab sequencing — 1 → 2 → 3 | Accepted |
| [0008](0008-csp-build-time-injection.md) | CSP injected at build time (dev server runs relaxed) | Accepted |
| [0009](0009-centered-symmetric-lag-explicit-zero-index.md) | Centered symmetric lag with explicit zero-index | Accepted |
| [0010](0010-idealized-recovered-kernel-open-family-toggle.md) | Idealized-recovered kernel is an open family toggle | Accepted |
| [0011](0011-validation-gates-machinery-not-fit.md) | Validation gates machinery, not fit | Accepted |
| [0012](0012-timing-vector-authoritative-dt-derived.md) | Timing vector is authoritative; dt is a derived fallback | Accepted |
| [0013](0013-binned-count-pre-first-bin-regime.md) | Binned-count pre-first-bin regime — teaching-keep default, validation-drop opt-in (`preFirstBin: 'keep'\|'drop'`); v1 stand-in for v2 buffered window | Accepted |
| [0014](0014-machinery-check-metric.md) | Machinery-check metric — causal-lobe / peak-lag+τ+amplitude diagnostics, raw whole-kernel correlation avoided as headline; gate is human, math is guide | Accepted (metric set provisional) |
| [0015](0015-harness-noise-model.md) | Noise model — v1 AWGN with a user slider 0–10× cohort-typical σ (1× ≈ 0.0024, from baseline regions); realizes §7/§11.2 noise injection; region-conditioning / σ-distribution / shot-term deferred to v2 | Accepted (v1 scope) |
| [0016](0016-csv-input-layout.md) | CSV input layout — per-region `time` / `spikes` (ragged) / `roi1..roiN`; produced offline by `scripts/mat2csv.py`; closes §10 item 6 | Accepted |
| [0017](0017-circular-deconv-zero-padding-no-fix.md) | Circular-deconv zero-padding needs no fix; detrend disposition is by anchor (endpoint/window harmful, quiet-anchored safe display-only); real-data bowl is decoupling + Laplacian low-freq blindness beneath a recovered kernel, not padding | Accepted |
| [0018](0018-figure-gate-policy.md) | Figure-gate policy — the human eye is the instrument; figure reads outrank summary statistics and second opinions when they conflict; operational extension of ADR-0014 | Accepted |
| [0019](0019-tab2-input-contract-workbook-per-recording.md) | Tab 2 input contract — one xlsx workbook per recording (`trace`/`spikes`/optional `metadata` sheets); whole untrimmed recording on a zero-based shared origin (t=0 at experiment onset), regions defined in-app (disjoint, overlap = hard error) and bracketed at analysis time, spike-bracketed default region; spike-sufficiency reported not gated; CSV (two files) as field fallback — **§6's two-file path was never implemented; the app still reads ADR-0016's single rectangle, see [ADR-0038](0038-input-template-working-example-recording.md) §4**; replaces ADR-0016 layout (0016 stays Accepted); SheetJS becomes v1 critical-path. Accepted = core contract (frozen v1.0); open tails remain | Accepted |
| [0020](0020-region-end-markers-raw-no-clamp.md) | Region-end markers emitted raw — finite overhang past `tEnd` is **not** clamped (source-faithful `exp_timing×60`); open-ended `inf` ends fall back to `max(timing)` (representability exception, not a clamp); consumers must not assume `end_s ≤ tEnd`; ratifies exporter behavior, refines ADR-0019 | Accepted |
| [0021](0021-kernel-recovery-three-parallel-methods.md) | Kernel recovery as three parallel methods, all retained, spread-is-diagnostic — (1) free-vector = existing Tikhonov+Laplacian-prior (ADR-0004), raw, bowl is information; (2) constrained-parametric double-exponential (not alpha), zero-baseline by construction, makes decay τ first-class, fails loud; (3) shape-regularized = extend the existing Laplacian penalty with baseline-flatness + acausal-energy terms (smoothness already exists). Post-hoc cleanup rejected (figure-gate). Build order free→parametric→shape-reg, incremental; canonizes design only (implementation pending, not the decision) | Accepted |
| [0022](0022-no-ap-silent-recording-policy.md) | No-AP (silent-recording) policy — zero-spike regions/recordings are handled by policy, not recovery: flag-on-read (per-region `zeroSpkRegions`, already in `golden/_batch_summary_v1.csv`), batch-SKIP zero-AP regions (no recovery, no batch error), single-file no-AP → "no APs in this recording — deconvolution not possible". Zero spikes is upstream of fit (no input to convolve), NOT a low-fit report (boundary vs ADR-0011); silent regions excluded from the decoupling-incidence denominator, never scored as decoupled. Scope: 33 silent + 22 partial across the 72-file batch. GATES the 9-column re-fan | Accepted |
| [0023](0023-method3-baseline-strategy.md) | Method-3 baseline strategy — RESOLVES the fork by adopting **both** as terms in one combined objective: a kernel-smoothness (Laplacian) penalty plus **baseline-flatness + acausal-energy** penalties on the kernel, AND a **minimal low-order (degree-2) drift nuisance basis**, jointly fit. The two attack the drift⇄τ_decay confound from opposite sides — the basis gives drift a home, the penalties keep the kernel from absorbing it. Figure-gated (ADR-0018) on three instruments: synthetic oracle (τ_decay within ~0.3 s of planted; basis does not steal decay at degree 2), high-noise sweep (τ_decay unbiased as σ→30×, only spread grows; drift over-fit flat at ~3.9×, structural not noise-amplified), file-80 ROI-1 graduation (+0.60 s peak; baseline flattened −0.0099→+0.0011; τ_decay finite 3.89 s where free-vector returned NaN). Cautions: recovered drift ~3.9× true (a kernel-protection device, NOT a trustworthy drift measurement); the minimal basis is load-bearing; a longer-τ (~5–6 s) oracle — where the confound bites hardest — is flagged follow-up | Accepted |
| [0024](0024-kernel-sta-overlay-display-mode.md) | Kernel/STA overlay amplitude axis — a tab-local display TOGGLE: (1) SHARED-Y default (true relative magnitude, the honest baseline, always seen first) + (2) NORMALIZED overlay opt-in (both traces to a common reference so cross-method SHAPE is legible when a magnitude gap swamps it — display-only). Twin-y (dual independent axes) REJECTED: non-coincident zero lines + independent scaling can manufacture false cross-method agreement (the read ADR-0005 exists to make honest). Safety: normalized mode must be unmissably labeled, and kernel peak amplitude stays reported NUMERICALLY in both modes (§3 plausibility leg — figure shows shape, readout shows magnitude). Resolves the parked shared-y-vs-twin-y item from the re-fan magnitude-gap evidence; MATLAB-figure check no longer a prerequisite. Builds on ADR-0009 (shared zero-lag x-axis) | Accepted (extended by [ADR-0029](0029-overlay-scale-kernel-sta-targets.md)) |
| [0025](0025-tab2-indicator-column-railed-fit-display.md) | Tab 2 indicator column + railed-fit display — facts for the eye, never a verdict. The machine reports computed MECHANICAL FACTS via a neutral indicator column (no pass/fail color, no "fit failed" language); the human gates all decon success/failure (ADR-0011/0014). Two facts defined (extensible): parametric **τ-RAILED** (a fitted τ pinned to its bound — the ADR-0021 method-2 loud failure) and kernel **PEAK-AT-BOUNDARY** (max sample at the window edge → peak-lag readout is not a transient peak). Railed parametric output is **default-HIDDEN with a reversible "show anyways"** (a convenience, not a verdict). Free-vector NEVER gets an automated failure flag (no reliable decon-success criterion; an FV "fit failed" detector would assert the coupling verdict the tool leaves to the human — out of bounds). Stance: machine states mechanical facts, suppression stays human-reversible; a future auto-flag/hard-hide would violate ADR-0011/0014 | Accepted |
| [0026](0026-tab2-layout-left-rail-three-plot-bands.md) | Tab 2 layout — workflow-staged left rail (~300px) holds all controls + the four §3 checks (as a compact stat readout, "numbers; figures are the instrument"); file management auto-collapses post-load; λ/noise live in an "Advanced" fold (§11.1 tiering). Right column = three CO-EQUAL-height plot bands (reconstruction / spike raster / kernel+STA overlay), full vertical extent. **Spike raster promoted to first-class** — equal absolute height (it is the recovery input), retiring the prior ~62/38 consolidated-panel split; binned-count + pinned [0,maxCount] retained, stems as filled bars for sparse-cell legibility. Recovers the top-third chrome so all plots clear the fold on a 16" fullscreen viewport. Settles the Tab 2 case of FOUNDATIONS §11.4; relocates (not changes) ADR-0024/0025; display-only, no core change; validated by a Tony-confirmed file-301 mock (ADR-0018) | Accepted (band arrangement superseded by [ADR-0033](0033-shared-plot-shell-square-kernel.md); rail/raster/numbers intent intact) |
| [0027](0027-subwindow-recovery-region-view-mode.md) | Sub-window recovery is first-class + region view mode — corrects the NEXT_SESSION HORIZON note, which over-broadly fenced ALL sub-window recovery as future V2. Keeps two operations distinct: **view-zoom stays view-only** (inspecting never recomputes kernel/STA/§3 — the honesty line holds) vs. **sub-window recovery is a first-class scientific operation** central to the §4 thesis (kernel is a property of the span it is recovered over; a clean-coupled-span kernel reveals the AP→Ca²⁺ model the contaminated whole-recording average masks). Two flavors, same machinery: region selection (ADR-0019 protocol epochs) and coupling-window selection. Whole-recording and window kernels are **co-equal in legitimacy** — the gap between them IS the decoupling measurement (machinery-gated/fit-reported §3, facts-not-verdicts ADR-0025; show both + gap, human reads contamination). **Region view mode** (whole-recording default with region overlay markers vs. region mode = one region at a time, recon/raster/§3/kernel windowed region-local, prev/next nav) + a **kernel-band overlay toggle** independent of view mode (all-regions vs. current-region, inherits ADR-0024 amplitude policy). Per-window spike sufficiency reported, not gated (ADR-0019). Method 3 (ADR-0023) drops in later unchanged. Supersedes the HORIZON note; FOUNDATIONS §4/§11.4 to follow in a separate canon commit | Accepted (§3 superseded by [ADR-0028](0028-regional-only-kernels-zoom-driven-selection.md); §1/§2 intact) |
| [0028](0028-regional-only-kernels-zoom-driven-selection.md) | Regional-only kernels; Mode toggle removed; zoom-driven region selection — supersedes ADR-0027 §3. A whole-signal kernel across >1 region is a CONTAMINATED cross-epoch average (neither baseline nor any treatment kernel) that answers no scientific question (FOUNDATIONS §3 principle); only REGIONAL kernels/STA are informative. So: (1) the whole-recording kernel is NEVER rendered when a recording exceeds the baseline region (single-region is the boundary case where whole=regional, shown normally); (2) the Whole/Region **Mode toggle is removed** — region selection is the existing double-click-to-region zoom (default = full recording, all regions shaded+labeled, kernel band shows all regions none highlighted; double-click zooms + makes that region current/bold; single-click resets; drag zooms); (3) **prev/next nav removed** (pure-click); (4) kernel-band **Current/All toggle retained** (the one choice zoom can't make — current region alone vs. cross-epoch comparison; inherits ADR-0024). ADR-0027 §1/§2 (sub-window recovery first-class; view-zoom view-only) UNCHANGED. Sharpens the §4 "is there any kernel" framing to per-region (whole-instrument role survives only for single-region recordings). Canon only; the Mode-removal/layout code pass follows | Accepted (§3 checks relocated rail→summary by [ADR-0033](0033-shared-plot-shell-square-kernel.md); region model intact) |
| [0029](0029-overlay-scale-kernel-sta-targets.md) | Overlay-scale gains kernel/STA scale-target modes — EXTENDS ADR-0024 (does not supersede; twin-y rejection reaffirmed). The Overlay-scale control becomes a single 4-way choice, all on ONE shared y-axis, differing only in whose range sets it: (1) **Shared-y** default (all curves), (2) **Normalized** opt-in (each curve to its own peak; shape-only), (3) **Scale to kernels** (axis range from the kernel curves; STA overflows/clips honestly), (4) **Scale to STA** (range from STA curves; kernels sit small but on the same axis). Modes 3-4 pool by curve TYPE across regions (per curve-type, not per region). Motivated by high-rate data (file-233: STA swamps kernel on shared-y; normalized drops the amplitude relationship). NOT twin-y: single axis with selectable range — STA overflowing a kernel-scaled axis is the TRUTH, not a faked second axis. §3 check-4 agreement is read from readout numbers + peak lags, not apparent visual co-height (numbers unaffected by display scale). ADR-0024 stays Accepted | Accepted |
| [0031](0031-tab1-forward-noise-injection.md) | Tab 1 measurement-noise tool — AWGN injected on the forward-convolution OUTPUT (measurement noise on the synthesized dF/F₀ trace, σ in dF/F₀ per ADR-0015); clean teal + faint noisy realization OVERLAID in the existing output band (shared `xs`/y-scale via `Plot.svelte` `ys2`/`color2`, so ADR-0030 coupled zoom/co-registration is untouched); control = the ADR-0015 0–10× cohort-typical σ slider (default 0/off, surfaced not Advanced) + a peak/σ SNR readout + a seeded reseed button (stable-but-refreshable realization via `mulberry32`). No change to `noise.js` (v1 AWGN + baseline-region σ stand; region-conditioned/shot terms stay v2-deferred). Realizes settled FOUNDATIONS §7/§11.2 (no canon change); pure JS, no new deps, privacy posture untouched | Accepted (default-0 superseded by [ADR-0042](0042-noise-on-by-default-kernel-band-key.md); rest intact) |
| [0032](0032-tab1-kernel-amplitude-control.md) | Tab 1 kernel amplitude control — a user-set **peak-height (dF/F₀)** axis that makes the ADR-0031 noise tool useful. Builders emit a peak-1 shape; `buildKernel` gains a 4th arg `amplitude` (default 1 → existing callers + peak-1 core tests byte-identical) that scales it in the ONE place kernel height is set, so output/kernel-plot/SNR all inherit the scale and the unit-spike-reproduces-kernel identity holds. ONE universal `kernelAmp` control (0.01–1 slider beside the shape params), NOT a per-kernel param — peak height is common to all shapes and must survive a shape switch (per-kernel params reset via `defaultParams`). UI default **0.1** opens Tab 1 where physiological σ bites (peak 0.1 → 10× noise gives SNR ≈ 4, visibly degraded; was SNR ≈ 42 at peak 1, invisible); noise still defaults off (§11.2). Realizes the "separate, user-set amplitude axis" ADR-0031 already named (no canon change); pure JS, no new deps | Accepted |
| [0033](0033-shared-plot-shell-square-kernel.md) | Shared 20/80 plot shell across BOTH tabs (`Shell.svelte`) — a 20% tools rail + 80% plot area whose top row is a summary panel beside a SQUARE top-right kernel (`aspect-ratio:1`), with full-width co-registered time-course bands below; content per-tab via rail/summary/kernelPanel/bands snippets. Readouts (not a time band) sit beside the kernel BY NECESSITY — a narrower band there would break the ADR-0030/§11.5 pixel-for-pixel x co-registration invariant (preserved intact). Tab 1 gains the rail (controls folded in) + becomes full-height; Tab 2 re-slots (§3 checks move rail-bottom→summary, kernel+STA Band C→square, recon+raster stay full-width bands). Shared nav-row width toggle (`wide`: capped 1600px ↔ full-bleed; plots re-fit via Plot's ResizeObserver). Tab 1 defaults → 300 s / 10 Hz / seeded random 0.1 Hz Poisson spikes. **Supersedes the band ARRANGEMENT of ADR-0026 + the §3 PLACEMENT of ADR-0028** (region model intact); display-only, no core; 202/202 | Accepted |
| [0034](0034-tab1-tab2-handoff.md) | Tab 1 → Tab 2 handoff ("Recover this in Tab 2 →") — realizes the settled-but-unbuilt cross-tab flow (FOUNDATIONS §11.3). A Tab 1 summary button builds a CSV in memory (time/spikes/dFF0 from output.times + known spikes + the measurement fluorescence — noisy realization when noise is on, else clean) and loads it through Tab 2's EXISTING `loadCsv` path (no new ingestion code, byte-identical recording shape). One-shot via `handoff`+`onConsumed` (Tab 2 remounts on tab switch; clearing prevents re-clobbering a user-loaded file). Closes the ground-truth loop the tool exists for (known kernel → synthesize → recover → compare); verified headless: 0.0 fluorescence error, exact spikes, no warnings. §11.3 realized for Tabs 1→2 (Tab 3 + chosen-kernel sharing still open, §11.4); privacy intact (in-memory, no new deps) | Accepted |
| [0035](0035-region-protocol-windowing.md) | Region protocol windowing — the app derives a per-region analysis window from the raw markers + region **name**, before the ±1 s spike-bracket. **baseline** = last `MAX` anchored at the END (`[end−MAX, end]`); **treatment** (any non-baseline, non-hiK switch) = wash-in delayed start then up to `MAX` (`[start+DELAY, min(end, start+DELAY+MAX)]`); **hiK** (`hik`/`highk`) = entire period raw (acts fast); **full** (synthetic `(full recording)`/`whole`) = raw passthrough. Defaults `DELAY=2min / MIN=12min / MAX=20min`, **user-adjustable** in Tab 2's Advanced fold (shown only with metadata regions). Never silently drops: sub-`MIN` durations are flagged-but-kept; only a treatment shorter than `DELAY` is reported non-analyzable. Closes the "APs in TTX/treatment" wash-in leak (golden 98: 382 APs trimmed from `sb222200`). Opt-in (`windowRegion` `protocol` flag, default off) so synthetic/CSV/handoff paths stay byte-identical. **Supersedes ADR-0019 §4 windowing** (raw markers still the emitted contract); needs bus contract **v1.1** clarification (names semantic, app owns windowing — no golden re-emit). 217/217, real-data probe confirmed | Accepted |
| [0036](0036-sheetjs-install-from-maintained-tarball.md) | SheetJS pinned to the maintained CDN tarball (`https://cdn.sheetjs.com/xlsx-0.20.3/…`), not the frozen npm package. `npm install xlsx` resolved to **0.18.5** — the version frozen when SheetJS **left npm in 2023**; its two upstream-fixed CVEs (prototype pollution GHSA-4r6h-8v6p-xvw6 → 0.19.3; ReDoS GHSA-5pgg-2g8v-p4x9 → 0.20.2) only ever shipped via the vendor CDN, so `npm audit` showed 1 high, "No fix available". Corrects the **install source** left to default in ADR-0019 (the library choice was right). A CDN tarball is a **build-time** fetch → does **not** violate FOUNDATIONS §6 (no *runtime* egress; still bundled locally, `connect-src 'none'` intact). Result: `npm audit` clean, 8 fewer transitive packages, `load-xlsx` still code-split, both goldens parse identically. Reader unchanged (read/sheet_to_json stable 0.18→0.20). Bumps are now a deliberate URL edit, never `npm update` back onto the dead package | Accepted |
| [0037](0037-adopt-murderboard-review-process.md) | Adopt the murderboard — vendor the anti-slop document-review process + OA lit-fetch tool from `syncytium2/murderboard` (`docs/doc_review_process.md` + `tools/fetch_paper.py`, do-not-edit vendored copies updated by re-copy), plus a CLAUDE.md rule that document deliverables run through it. Dev-time only (no app import, no bundle, no npm dep) → FOUNDATIONS §6 untouched; FOUNDATIONS stays source of truth, murderboard is a process aid | Proposed |
| [0038](0038-input-template-working-example-recording.md) | The input template is a **working example recording**, generated in-browser — discharges the deliverable ADR-0019 §5 reserved as TBD. Not an empty skeleton: ~2 min of synthetic data (1200 frames @ 10 Hz, 2 ROIs, 2 regions) synthesized from the app's OWN core (`rasterize`→`buildKernel`→`convolveOnGrid`→`addAWGN`, seeded), so a user watches the format succeed **before** risking their own data, and the app gains a demo path through the REAL ingest (regions, multi-ROI, metadata sheet) that the ADR-0034 handoff cannot exercise. Region names `baseline` / `high K+` are chosen to survive ADR-0035 windowing — any other name classifies as a *treatment*, loses 120 s of wash-in, and at 60 s regions leaves NO window; `npm run template-acceptance` round-trips both files through the real `loadCsv`/`loadWorkbook` and pins this. Generated in-browser, never committed (one source of truth with core), at the cost of SheetJS's writer on the **lazy** xlsx chunk. **Also records a contract-vs-implementation divergence:** ADR-0019 §6's two-file paired CSV path was never built; `loadCsv` reads ADR-0016's single rectangle, which the CSV template targets. Same pass fixed Tab 2 having no keyboard path to load a file | Accepted |
| [0030](0030-shared-timebase-axis-co-registration-invariant.md) | Shared-timebase axis co-registration is a cross-tab invariant — generalizes the Tab 2 co-registration (ADR-0026) into a durable plotting rule and records the y-axis policy. (1) **x-axes for related time-base data MUST align pixel-for-pixel** — identical x-range AND identical plot-area geometry (equal left gutter + equal right padding), not merely a shared range; a shared-range-but-sheared-geometry pair was the Tab 1 bug (spike band hid its x-axis → uPlot reserved different right-edge space → spikes didn't drop onto their response). Stacked shared-time bands also get a **shared hover crosshair** (dashed time-line linked by data-x + per-series value-dots). (2) A **different** timebase (the kernel's ±win lag axis, ADR-0009) deliberately does NOT co-register — aligning axes of different meaning misleads. (3) **y-axes identical when possible/reasonable, human-decided** — a preference per the figure-gate (ADR-0018), not an enforced rule (shared-y where comparability matters, per-plot where a shared scale flattens detail). (4) **Tab 1 adopts the mechanism** (equal padRight + syncKey + cursorPoints on its spike/output bands, matching Tab 2). Display-only, no core change; 202 tests unaffected; validated by a Tony-confirmed screenshot. Settles FOUNDATIONS §11.5 | Accepted |
| [0039](0039-kernel-support-amplitude-cutoff.md) | Kernel support is cut on **amplitude**, not at a fixed σ/τ — one `TAIL_EPS = 1e-3`-of-peak threshold replaces the per-shape ±3σ / 5τ / 5·τ_decay rules, which bounded *distance* but not the **step discontinuity** left at the end of the array (measured 1.11% / 0.67% of peak, and **2.68% at the Tab 1 calcium defaults**, up to **9.14%** as τ_rise → τ_decay, where the difference-of-exponentials tends to t·exp(−t/τ) and its 5τ tail is an order of magnitude fatter). A step is broadband → seeds ringing in the Tab 2 FFT decon (ADR-0004/0006); the default kernel also *looked* wrong, cornering at 2.00 s. Factors become the threshold solved for t (Gaussian 3.72σ, exponential 6.91τ, calcium scanned forward **from the peak index** — k(0)=0 is sub-threshold but is not the tail — under a non-binding 12·τ_decay guard), and every support rounds **outward**, never to nearest (half a sample short doubled the Gaussian step). **Boxcar excluded** — its edges are the shape (ADR-0003 hand-verify object). Truncation kept over tapering: a taper moves samples off the analytic curve Tab 1 claims to plot. Result: worst-case step **0.1% for all three shapes** across every slider position; default calcium 201→333 samples (2.00→3.32 s). Longer kernels are ~free — ADR-0006 convolution is sparse stamp-and-sum (cost = spikes × length). ADR-0009 contract untouched (only `samples.length` moves). `deconvolve-parametric.js`'s `TAIL_FACTOR = 5` deliberately **left alone** (different cutoff, different purpose — the fitted forward model's support; aligning it would move Tab 2 fit numerics). Tail-step invariant swept across all shapes/ranges in tests; 237/237, machinery + template acceptance OK | Accepted |
| [0040](0040-tab1-band-order-follows-tab0.md) | Tab 1 stacks **output above input**, following Tab 0's premise figure — the calcium trace is the object of interest (what an experiment measures, what Tab 2 inverts); the spike train is what produced it. Tab 0's figure and the Tab 1 challenge view (`FitTheTrace`) already stacked them this way, so the Tab 1 **Learn** view was the odd one out against a view one button away. Adopts Tab 0's arrangement: trace on top, x-axis + `time (s)` moving to the lower band (free to move — co-registration is held by equal `padRight`/`yAxisSize`, not by both bands drawing an axis), each band named on its **y-axis** (`dF/F₀` / `spikes`, `yAxisSize` 48→54 on BOTH), and the raster as a short strip since it carries one bit per sample. The one place Tab 0 could not be copied literally: its fixed 136px raster works in document flow but **inverts priority in a viewport** — measured, a fixed height left the trace *shorter* than the raster at ~1100px (122 vs 136) and 0px below the 900px breakpoint, so the strip is a **`max-height` cap** that shrinks with the trace. Co-registration verified not assumed (both plot areas x = 488…1619 @1728px). Deliberately unlike ADR-0026's co-equal Tab 2 raster (there it is the recovery *input*, here an illustration). **Colors deliberately NOT unified** — Tab 0's blue/red vs Tab 1's teal/neutral is a palette call, and Tab 1's teal is named in ADR-0031's user-visible copy; flagged for Tony. Display-only, no core, 237/237 | Accepted |
| [0041](0041-plot-series-palette-one-color-per-quantity.md) | **One color per quantity**, defined once as `--series-*` tokens in `app.css` — plot colors were hex literals spread across seven components, so nothing enforced that the same quantity got the same color twice, and it didn't: the calcium trace was blue on Tab 0 and teal on Tabs 1/2/3, the raster red on Tab 0 and ink elsewhere, and **red had drifted into three unrelated meanings** (Tab 0 spikes, Tab 2's reconstruction, "you" in Guess the spikes). Two collisions sat INSIDE one view — the Colonel drawn gray in one panel and *teal* in the next, "you" purple in one plot and red in another. Grammar: hue answers *whose* in comparison plots and *what* elsewhere — `--series-trace` teal (data/target), `--series-machine` red (Tab 2 recon, the Colonel), `--series-you` purple (the kernel you author, your attempt), `--series-spikes` ink, `--series-truth` gray + ALWAYS dashed. Teal beat Tab 0's blue on churn (blue appeared once; adopting it would force the recon off red, rewrite "teal = clean" and recompute `--noise-trace`) though blue/red scores better on CVD. Dark chart purple steps to `#b45ef5` — the dark UI `--accent` `#c084fc` is OKLCH L 0.72, above the L 0.48–0.67 band a series mark needs; chrome keeps the accent. Tab 2's **Okabe-Ito region hues untouched** (they encode *which region* — ADR-0024/0029 — a different axis). Palette **validated not eyeballed**: all six checks pass both surfaces (worst adjacent ΔE 13.1 deutan ≥ 8). Copy naming colors rewritten to describe **position** ("the ticks along the bottom"), incl. the figure's screen-reader alt; identity never color-alone (truth dashed, spikes up/down). CSS-var-on-canvas verified live in both schemes (`resolveColor()`, 3 tokens nest a var) — no `#888` fallbacks. Verified across 4 tabs + 3 challenges + both revealed states, light and dark. Fixes a stray "your kernelvs Colonel" spacing bug. 237/237 | Accepted |
| [0042](0042-noise-on-by-default-kernel-band-key.md) | **Tab 1 opens with noise ON (3× cohort σ, SNR ≈ 14)** + the kernel band gets a key beside it. **Amends canon** — §7 named noise injection *"so deconvolution isn't deceptively easy"* and then §11.2 set it to **0**, so Tab 1 opened noiseless and, via the ADR-0034 handoff, so did Tab 2: its headline read **retained-kernel R² 1.0000, acausal ratio 0.0000**, a result no recording produces, on the tab whose job is judging whether a recovery can be believed. Now **0.8920 / 0.0134** with visible ringing — the honest picture the §3 checks exist to interrogate; clean is one drag to 0. Level picked by eye (1× invisible at the 0.1 peak, 10× buries small events). Tab 2 half: the kernel+STA square had **no key** — one existed but ADR-0033 had moved it into the summary panel, below the fold, and it had **drifted** (still painting `#7b2ff7` after ADR-0041 moved kernels to `--series-you`; STA swatch always solid though the curve dashes only with a region hue). Key moves under the square, **derived from the same branch conditions and colors as the series** so it cannot drift again; numbers + railed toggle stay in the summary (ADR-0026 "numbers; figures are the instrument"); dashed curves get real dashed swatches. Side finding: flipping the default **exposed a latent text bug** ("dF/F₀ trace**with** measurement noise" — Svelte trims an `{#if}`'s leading space, unreachable while the default hid the branch); swept and fixed the other instance. Supersedes ADR-0031 §3's default only. FOUNDATIONS §7 + §11.2 updated. 237/237, no core change | Accepted |
| [0043](0043-tab3-overlay-true-and-recovered-input.md) | Tab 3 **superimposes true spikes and recovered input** in one band. Three equal-height bands meant the bottom one — the only one drawing the x-axis + `time (s)` label, ~60px of uPlot chrome — got **8px of plot** (measured 71/71/**8** live): illegible, and actively misleading, since Tony read the recovered trace as having **negative** deflections on the spikes and asked if the math was broken. **It wasn't** — checked directly, the recovered value at all 33 true spike times is **positive** (+0.23…+0.29); negatives (min −0.067) fall only *between* spikes, which is the regularized inverse's ringing this tab exists to show (§2). A collapsed plot can invert apparent sign. Fix: overlay the two on **one shared y-axis** — and that axis is the *point*, not a compromise, because the recovered estimate is in the same units as the input, so the reader sees directly that naive deconvolution returns ≈**0.3×** a unit spike, smeared, with ringing (exactly what `PEAK / TRUE ×0.3` and `RECOVERED < 0 47%` report). Two auto-scaled axes had been HIDING that gap by drawing both series full-height. Records a reusable layout rule: **the band carrying the axis must be paid for it** — `flex: 1 1 60px` vs `1 1 0` equalizes the PLOTS, not the containers (after: 123/119px). Inline key added (two series ⇒ never color-alone). Co-registration verified (both plots x = 416…1535). 237/237, no core change | Accepted |
| [0044](0044-convolution-scatter-form.md) | **`convolveLinear` accumulates by scatter — the definition, reordered.** Records existing behaviour; no code change. The "stamp and sum" claim ("exactly the teaching description") lived only in a source comment: no ADR covered the **formulation** (ADR-0006 decided linear-vs-circular, a different axis), and the suite pinned one hand-computed example, which catches a *broken* implementation but not a subtly *different* one. The textbook form is a **gather** (walk output `n`, sum `x[k]·h[n−k]`); `convolve.js` uses the **scatter** (walk input `i`, add `x[i]·h[j]` into `out[i+j]`). Same products, same partition by `i+j` — loop reordering, not approximation — and kept because it is both the sparse-efficient shape for spike trains and literally the sentence the tab teaches. The one non-literal line is `if (xi === 0) continue`: exact for finite values, would diverge only on `Inf`/`NaN` kernels (`0 × Inf = NaN`), which the builders never emit. Now tested as a **property**: diffed against a from-the-definition reference over 8 shape pairs of **dense random** values (no zeros ⇒ sparse skip never fires, FP ordering maximally exposed). Asserts agreement to rounding (rel ≤ 1e-12), **not** bit-identity — the two are bit-identical today and the test reports it, but a legitimate reordering may cost an ulp without being wrong. Notes where the real approximations live instead: spike snapping (ADR-0001), kernel truncation (ADR-0039), FFT zero-padding (ADR-0006/0017). 240/240 | Accepted |
